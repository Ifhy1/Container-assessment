apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: much-todo-ingress
  namespace: much-to-do
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: much-todo-api
            port:
              number: 8080
